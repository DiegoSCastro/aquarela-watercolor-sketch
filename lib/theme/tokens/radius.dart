/// Border radius scale — soft, organic, never sharp.
class RadiusToken {
  const RadiusToken._();

  /// 6 — badges, chips.
  static const double sm = 6;

  /// 12 — buttons, small cards.
  static const double md = 12;

  /// 20 — large cards, modals.
  static const double lg = 20;

  /// 32 — onboarding illustration containers.
  static const double xl = 32;

  /// 9999 — pills, FABs, circles.
  static const double full = 9999;
}
